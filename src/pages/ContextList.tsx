/**
 * Screen 1: every available conversation context as a card.
 *
 * OWNER: task B.
 * Done when: cards render from index.json (never hard-coded), each shows
 * context id, participant, turn count and date, and clicking one routes to
 * /context/:contextId.
 */
export default function ContextList() {
  return <main className="page">{/* TODO(task B) */}</main>;
}
