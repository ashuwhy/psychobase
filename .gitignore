node_modules/
dist/
.DS_Store
*.local
.vite/
.playwright-mcp/
__pycache__/

# The datasets live in website/public/data and are versioned there - the repo is
# private and limited to the team. website/public/data/build is generated by
# `npm run data`, but is committed because the Vercel build runs vite only, not
# the Python step.

# Lab network details stay out of git even though the repo is private - they are
# the department's infrastructure, not this project's.
model/SERVERS.local.md
.venv-smoke/
model/runs/
