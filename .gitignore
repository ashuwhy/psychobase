node_modules/
dist/
.DS_Store
*.local
.vite/

# Source datasets are versioned so everyone is provably on the same data -
# the repo is private and limited to the four of us plus Stiti.
# The build output is derived from them, so it is not versioned: run
# `npm run data` after cloning. See public/data/README.md.
public/data/build/
